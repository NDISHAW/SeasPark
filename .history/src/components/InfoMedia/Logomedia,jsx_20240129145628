import React from 'react';

const Logomedia = ({number, image, icon, title, info, className }) => (
  <div className={`info-media ${className || ''}`}>
      {image && <img src={image} alt="logoicon"/>}

  </div>
);

export default Logomedia;